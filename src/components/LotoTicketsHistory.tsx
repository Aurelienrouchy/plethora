import React from 'react';
import { View } from 'react-native';

interface LotoTicketsHistoryProps {
    id: number
}

const LotoTicketsHistory = ({ id }: LotoTicketsHistoryProps) => {
    return (
        <View>

        </View>
    );
}

export default LotoTicketsHistory